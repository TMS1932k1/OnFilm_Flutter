import 'package:flutter/material.dart';

class CirculaProgressLoading extends StatelessWidget {
  const CirculaProgressLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
